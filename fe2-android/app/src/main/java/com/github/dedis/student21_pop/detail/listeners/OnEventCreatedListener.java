package com.github.dedis.student21_pop.detail.listeners;

import com.github.dedis.student21_pop.model.event.Event;

public interface OnEventCreatedListener {

  void OnEventCreatedListener(Event event);
}
